import { AppChbgcolorDirective } from './app-chbgcolor.directive';

describe('AppChbgcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppChbgcolorDirective();
    expect(directive).toBeTruthy();
  });
});
